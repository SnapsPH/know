# Ollama Configuration
# URL of the Ollama server (default: http://localhost:11434)
OLLAMA_URL=http://localhost:11434

# Ollama model to use for knowledge processing (default: llama3.2:3b)
OLLAMA_MODEL=llama3.2:3b

# Crawler Configuration
# Maximum crawl depth (default: 3)
MAX_CRAWL_DEPTH=3

# Maximum number of pages to crawl (default: 100)
MAX_CRAWL_PAGES=100

# Storage Configuration
# Base path for storing raw and processed data
# If starts with '/', it will be relative to the project root
BASE_STORAGE_PATH=/knowledge_retrieval

# Optional: Specify a custom user agent for web crawling
USER_AGENT=KnowledgeRetrievalBot/1.0

# Optional: Timeout for HTTP requests (in milliseconds)
REQUEST_TIMEOUT=10000
